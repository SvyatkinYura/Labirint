
struct Corvo
{
    double x, y;
    double respX, respY;
    int hasSword; // 1 - с оружием, 0 - без
    int hasBarrel; // 1 - с бочкой, 0 - без
    int levelComplete; //1/0
    int killguard;
    HDC texture;
};